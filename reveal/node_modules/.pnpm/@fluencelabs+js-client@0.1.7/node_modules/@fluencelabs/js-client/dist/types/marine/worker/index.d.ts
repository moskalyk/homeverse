import type { JSONArray, JSONObject, CallParameters } from '@fluencelabs/marine-js/dist/types';
import { IMarineHost, IWasmLoader, IWorkerLoader } from '../interfaces.js';
export declare class MarineBackgroundRunner implements IMarineHost {
    private workerLoader;
    private controlModuleLoader;
    private avmWasmLoader;
    private workerThread?;
    private loggers;
    constructor(workerLoader: IWorkerLoader, controlModuleLoader: IWasmLoader, avmWasmLoader: IWasmLoader);
    hasService(serviceId: string): Promise<boolean>;
    removeService(serviceId: string): Promise<void>;
    start(): Promise<void>;
    createService(serviceModule: ArrayBuffer | SharedArrayBuffer, serviceId: string): Promise<void>;
    callService(serviceId: string, functionName: string, args: JSONArray | JSONObject, callParams: CallParameters): Promise<unknown>;
    stop(): Promise<void>;
}
